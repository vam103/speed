# Add One to Environment

Add one to the value of a key in an environment if exists, otherwise set
one to the key.

## Usage

``` r
env_add_one(env, key)
```

## Arguments

- env:

  An environment

- key:

  A key in the environment
