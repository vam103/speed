# Generate neighbour for simple (non-hierarchical) designs

Generate neighbour for simple (non-hierarchical) designs

## Usage

``` r
generate_single_swap_neighbour(
  design,
  swap,
  swap_within,
  swap_count,
  swap_all_blocks
)
```
