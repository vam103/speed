# Generate neighbour for sequential or hierarchical designs

Generate neighbour for sequential or hierarchical designs

## Usage

``` r
generate_multi_swap_neighbour(
  design,
  swap,
  swap_within,
  swap_count,
  swap_all_blocks
)
```
